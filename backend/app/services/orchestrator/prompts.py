"""
AI Bid Assistant - Prompt Templates

All prompt templates for the bid generation pipeline.
Variable placeholders use Python format syntax: {variable_name}
"""

# =============================================================================
# P1: 招标文件解析 Prompt
# =============================================================================
P1_PARSE_BID = """你是一位资深招投标专家。请仔细阅读以下招标文件内容，提取关键信息并以JSON格式返回。

## 要求
1. **评分标准表**：提取所有评分项，包括：评分项名称、满分值、评分标准描述、所需证明材料
2. **废标条款**：提取所有可能导致废标的条款（一票否决项），高亮标记
3. **技术参数要求**：提取所有带"★"或明确标注"必须满足"的技术指标
4. **商务资质要求**：提取所有资质证书、业绩、人员要求

## JSON输出格式
{{
  "score_items": [
    {{
      "name": "技术方案完整性",
      "max_score": 25,
      "criteria": "技术方案应包含系统架构设计、功能模块设计...",
      "required_proof": "系统架构图、功能清单"
    }}
  ],
  "reject_clauses": [
    {{"clause": "投标报价超过采购预算", "location": "第三章第2.1条"}}
  ],
  "tech_requirements": [
    {{"parameter": "系统并发用户数", "value": "≥5000", "mandatory": true}}
  ],
  "qualification_requirements": [
    {{"type": "资质", "name": "ISO9001质量管理体系认证", "mandatory": true}}
  ]
}}

## 招标文件内容
{bid_file_content}"""


# =============================================================================
# P2: 应标大纲生成 Prompt
# =============================================================================
P2_GENERATE_OUTLINE = """你是一位资深{industry_context}投标经理。根据以下评分标准，为应标文件生成对标响应大纲，以JSON数组格式返回。

## 原则
- 每个评分项必须有对应的响应章节
- 高权重评分项（>15分）可拆分多个章节
- 低权重评分项（<5分）可合并到相关章节
- 章节顺序遵循招标文件要求的顺序
- 每章标注：对应评分点、权重级别(high/medium/low)、所需附件清单

## 评分标准
{score_items_json}

## 企业基本信息
- 公司名称：{company_name}
- 核心业务：{core_business}
- 主要资质：{qualifications}

## JSON输出格式
[
  {{
    "section_id": "s1",
    "title": "项目理解与总体方案设计",
    "score_point_ref": ["技术方案完整性(25分)", "技术方案创新性(5分)"],
    "weight": "high",
    "required_attachments": ["系统总体架构图", "技术选型对比表"]
  }}
]"""


# =============================================================================
# P3: RAG检索Query构造 Prompt
# =============================================================================
P3_CONSTRUCT_QUERY = """根据以下应标章节的标题和评分要求，构造3-5个检索查询，用于从企业知识库中检索相关的历史标书内容。

章节标题：{section_title}
评分要求：{score_criteria}
公司名称：{company_name}

输出：每行一个检索query，尽量多样化（从技术方案、项目实施、公司经验等不同角度）"""


# =============================================================================
# P4: 标书章节内容生成 Prompt
# =============================================================================
P4_GENERATE_SECTION = """你是一位资深{industry_context}标书撰写专家。请基于以下信息，撰写应标文件的"{section_title}"章节。

## 写作要求
- 语言：专业、准确、简洁，使用招投标正式书面语
- 篇幅：{target_length}字左右
- 重点：紧扣评分标准，明确展示企业优势
- 禁止编造：所有资质、证书、项目案例、人员信息必须来自提供的"企业参考素材"
- 如果某项信息在企业素材中找不到，标注【待补充】，绝不自创
- 使用"我公司"自称

## 本章评分标准
{score_criteria}

## 企业参考素材（来自知识库检索）
{retrieved_content}

## 公司基本信息
- 公司名称：{company_name}
- 相关资质：{qualifications}

请直接输出章节正文，无需标题。"""


# =============================================================================
# P5: 合规检查 Prompt
# =============================================================================
P5_COMPLIANCE_CHECK = """你是一位招投标合规审查专家。请对比检查以下两份内容，以JSON格式返回检查结果。

## 招标文件关键要求
{parsed_requirements}

## 应标文件全文
{generated_bid_content}

## 检查项
1. **评分点覆盖检查**：逐一检查每个评分项是否有对应的响应内容
2. **废标条款检查**：逐条比对废标条款，确认应标文件是否满足
3. **敏感内容检查**：检查是否出现以下违规内容：
   - 其他公司名称（误用竞争对手名称）
   - "保证中标""绝对满足"等不当承诺
   - 明显虚假的资质信息

## 输出格式
{{
  "score_coverage": {{
    "covered": 85, "total": 100,
    "missed": [{{"item": "售后服务方案", "score": 10, "reason": "未找到对应章节"}}]
  }},
  "reject_check": {{
    "all_passed": true,
    "warnings": [{{"clause": "投标有效期不少于90天", "status": "已响应"}}]
  }},
  "sensitive_issues": []
}}"""


# =============================================================================
# Registry: name -> prompt template mapping
# =============================================================================
PROMPT_REGISTRY = {
    "P1_PARSE_BID": P1_PARSE_BID,
    "P2_GENERATE_OUTLINE": P2_GENERATE_OUTLINE,
    "P3_CONSTRUCT_QUERY": P3_CONSTRUCT_QUERY,
    "P4_GENERATE_SECTION": P4_GENERATE_SECTION,
    "P5_COMPLIANCE_CHECK": P5_COMPLIANCE_CHECK,
}
